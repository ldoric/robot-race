#include <iostream>

namespace gs{

  int strComp(std::string first, std::string second) // takes in 2 strings and returns: 1 - first bigger, -1 - second bigger, 0 - equal
  {
    return;
  }

  int strLen(std::string str)  // takes in 1 string and returns its length as an int
  {
    return;
  }

  std::string strRev(std::string str) // takes in 1 string and returns it reversed, it doesn't modify the first string
  {
    return;
  }

  void strCopy(std::string destination, std::string source) // takes in two strings and copies second into the first, returns nothing
  {

  }
  
}